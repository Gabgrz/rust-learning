fn main() {

    // Immutable variable
    let x = 1; 

    // Immutable variable with explicit type
    let z: bool = true;

    // Mutable variable
    let mut y = 2;

    // Assign new value to 'y'
    y = 3;

    println!("{}", x);

}
